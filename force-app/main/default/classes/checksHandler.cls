public class checksHandler {

    public static void handle(List<Account> newaccountList,List<Account> oldaccountList){
        
         system.debug('new=>  '+newaccountList);
         system.debug('new=>  '+oldaccountList);
    }
}