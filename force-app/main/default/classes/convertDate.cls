public class convertDate {
    
    public static void forConvert(){
        String str='09/15/20';
        String str1= str.replaceAll('[^a-zA-Z0-9\\s+]', '');
        System.debug('str1-' + str1);
        for(Integer i=0;i<str1.length();i++){
            
        }
    }
}